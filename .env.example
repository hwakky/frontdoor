# Keep this file private. Never use VITE_OPENAI_API_KEY: VITE_* values are exposed to browsers.
OPENAI_API_KEY=your_openai_api_key_here
# Optional. Use the Codex model your OpenAI API project is allowed to access.
OPENAI_MODEL=gpt-5.3-codex
